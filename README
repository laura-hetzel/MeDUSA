----Object Desciptions/expectations----

MZ-OBJ: The standard "MZ Object"
DataFrame where:
  - Rownames = Float: MZ
  - Colnames = String: sample_names
  - Data     = Float: Intensities

mzT-OBJ: An "MZ Object" rebranded to represent scan-time.
DataFrame where:
  - Rownames = Float: MZ
  - Colnames = String: ScanTime
  - Data     = Float: Intensities

mzLog-OBJ: A log2 of "MZ Object."
DataFrame where:
  - Rownames = Float: MZ
  - Colnames = String: sample_names
  - Data     = Float: log2(Intensities)

Programatically: the methods for [mz-obj, mzT-obj, mzLog-obj] are all interchangeable....proceed with caution.

mzLong-OBJ: A pivot longer of an "MZ Object"
DataFrame where columns are:
  - Float : MZ
  - String: ScanTime
  - Float : intensity

METADATA: The standard "MZ Metadata object"
Dataframe where the columns are:
  - Integer: measurement
  - String : sample_name (Must match above)
  - String : type (i.e. cell, media_cell, solvent)
  - Integer: sample_number
  - String : phenotype
  - Integer: time
  - Integer: plate
  - Integer: cell_count
  - String : sampler
  - String : polarity
  - String : sampling_day
  - String : filtered_out (yes, no) #todo turn this into a boolean


----Project Structure----
file: [input object type]_[suggested order]_[category].R (i.e. "mz_7_post_processing.R")
 - public function : [input_object]_[category]_magic
    - suggested method to handle everything in the file category (i.e. "mz_post_process_magic")
 - public function : [input object]_[subcategory]_[description] (i.e. "mz_pp_pivotlonger")
 - private funciton: .[description]

file: z_[description].R
  - common methods written by ehetzel
file: zz_[description].R
  - common methods written by others


---- TODO Oct31
  -X docker image, including sumr
  -X fix trycatch in massdefect
  -X why missingness drop rows with value of 1
  -X mz_random_forest (stemcell)
  - iso_tag output, 1 filtered(mz_obj sans isos), one original (iso_obj with mappings) [sucks]
  -X pca variable bug [need data]
  -X volcano plot (look at hypoxia)
  -X t-test "FDR correct" (stemcell)
  -X hmdb
  -X heatmap
  -X quality check [bug in plots]
  -X metadata file naming validation
  -X extract_magic, all the parameters
  -X fix file managment when meta & data & scripts are seperate
  -X mass defect windows debugging
  -X alignment fix
  -X unitTests capability
  -L unitTests content
  -2 lower dependencies (catools, tibble)
  -X config file (half-ish)
  -2 optional correlation RFs (cor() is optional?)
  -X lipidmaps
  -X lipidmaps search
  -X search by csv (targeted)
  - blanksubtraction, validation (need better data) ...
  -Xbetter pheno/blank filtering
  -Xhandle meta neg/pos better
  - anova analysis
  -X ggpubr
  -2 reparameterize phenotype in randomforest
  -L fill out commoon adducts
  -L method descriptions
